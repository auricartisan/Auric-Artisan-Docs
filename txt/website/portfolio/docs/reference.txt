==============================================================================
PORTFOLIO REFERENCE
==============================================================================
Every control, option, command, message and limit in the Portfolio Builder,
the published page and the Portfolio Index.
Area: Website › Portfolio   Updated: 2026-09-25

TOP BAR (HTTPS://AURICARTISAN.COM/PORTFOLIO/)
=============================================

  Control:      Your portfolio, address, Not published
  What it does: Shows your published address and state.

  Control:      Command field (`Ctrl K`)
  What it does: "Add a section, change the look, publish…" — opens the list of
                builder commands.

  Control:      Section count, Saved / Saved to your account
  What it does: Document size and save state.

  Control:      Undo / Redo
  What it does: Step back / forward. Disabled when there is nothing to undo or
                redo.

  Control:      Templates
  What it does: Opens Start from a template.

  Control:      Preview
  What it does: Opens the finished page in a new tab.

  Control:      Export
  What it does: Download HTML, Download PDF, Export JSON, Import JSON…, Copy
                snapshot link, "Made with Auric Artisan" credit switch.

  Control:      Publish
  What it does: Opens the publish dialog; disabled while Check has blocking
                findings.

MODE TABS
=========

  Tab     | Contents
  --------+-----------------------------------------------------------------------
  Content | Header panel, section cards, Add a section.
  Style   | Typeface, Paper, Rhythm, Accent, from your library, Reset to defaults.
  Check   | Accessibility findings; a dot marks findings while you are elsewhere.

HEADER PANEL
============

  Control                       | Limit                   | Default
  ------------------------------+-------------------------+--------------------------
  Use the built-in header       | On / off                | On
  Name                          | 80 characters           | Follows profile
  Headline / tagline            | 120 characters          | Empty
  Email + Show                  | 120 characters          | Follows profile; Show off
  Location + Show               | 80 characters           | Follows profile; Show on
  Website + Show                | 200 characters          | Follows profile; Show on
  Avatar (image address) + Show | 500 characters          | Follows profile; Show on
  Avatar picker                 | Double-click the avatar | —
  Accent                        | Colour picker           | `#8A6D16` theme default

SECTION CARD CONTROLS
=====================

Drag handle, expand/collapse, Hidden badge, Hide from portfolio / Show in
portfolio, Move up, Move down, More actions (Move to top, Move to bottom,
Duplicate, Delete).

Bulk bar: Collapse all / Expand all; Bulk actions ▾: Expand all, Collapse all,
Show all sections, Hide all sections, Reverse order, Clear all sections.

SECTION TYPES (25)
==================

Heading, Text / Markdown, Raw HTML, Two columns, Image, Gallery, Button, Call
to action, Link list, Work / Projects, Stats, Feature list, Steps, Timeline,
Skills / Tags, Testimonial, Quote, Callout, FAQ / Accordion, Table, Embed,
Video (MP4), Code, Divider, Spacer. Fields: see Section types
[section-types.txt].

STYLE OPTIONS
=============

  Option   | Values                                                      | Default
  ---------+-------------------------------------------------------------+----------
  Typeface | Fraunces, Newsreader, Space Grotesk, Manrope                | Fraunces
  Paper    | Cream, White, Sand, Ink, No background                      | Cream
  Rhythm   | Tight, Even, Airy                                           | Even
  Accent   | A colour from your saved palettes, graded against the paper | `#8A6D16`

PREVIEW
=======

  Control            | Values                                                   | Default
  -------------------+----------------------------------------------------------+--------
  Width              | Fit, Desktop (1160 px), Tablet (800 px), Mobile (390 px) | Fit
  Fullscreen preview | On / off                                                 | Off

CHECK FINDINGS
==============

  Finding                                | Severity
  ---------------------------------------+---------------------------------------
  Text below AA (under 4.5:1)            | Blocking when well below, else warning
  Large text below AA (under 3:1)        | Blocking when well below, else warning
  A link with no text                    | Blocking
  A link that does not say where it goes | Blocking
  An image with no alt text              | Blocking
  Heading jumps from hN to hM            | Warning
  A target smaller than 24px             | Warning
  A colour this check could not read     | Warning

Filters: All, Blocking, Contrast.

PUBLISH DIALOG
==============

  Control:      Public at an address / Only people I send the link to
  What it does: Visibility.
  Default:      Public at an address

  Control:      Your address
  What it does: `auricartisan.com/p/` + 2 to 31 lower-case letters, numbers
                and hyphens; checked for availability.
  Default:      Empty

  Control:      Or take
  What it does: Suggested addresses.
  Default:      —

  Control:      What a shared link shows
  What it does: Share-card preview.
  Default:      —

  Control:      Ready to go
  What it does: Address, sections, name, headline checklist.
  Default:      —

  Control:      Publish / Copy the link
  What it does: Publishes, or copies a snapshot link in private mode.
  Default:      —

  Control:      Copy, Open, QR code
  What it does: For the live address.
  Default:      —

  Control:      Publish these changes
  What it does: Updates the live page.
  Default:      —

  Control:      Reach
  What it does: View count since publishing.
  Default:      —

  Control:      Let search engines list it
  What it does: Search engine listing.
  Default:      On

  Control:      List it in the Portfolio Index
  What it does: Directory listing; needs search listing on.
  Default:      On

  Control:      Allow a PDF download
  What it does: PDF button for visitors.
  Default:      On

  Control:      Show the view count
  What it does: View count on the page.
  Default:      On

  Control:      Unpublish → Yes, take it down
  What it does: Takes the page down; frees the address.
  Default:      —

  Control:      Close
  What it does: Closes the dialog.
  Default:      —

COMMANDS (COMMAND FIELD)
========================

  Group:    Sections
  Commands: Add a … section for every type; Start from a template

  Group:    Editing
  Commands: Undo, Redo, Edit the content, Turn the built-in header on / off,
            Set the name in the header, Set the headline, Set the header
            email, Set the header location, Set the header website, Set the
            header avatar

  Group:    Theme
  Commands: Change the look; one command per typeface, paper and rhythm, such
            as Paper — Ink

  Group:    Preview
  Commands: Preview: fit the column, Preview: check it at desktop, Preview:
            check it at tablet, Preview: check it on a phone, Preview full
            screen

  Group:    Export
  Commands: Download it as HTML, Download it as PDF, Export the document as
            JSON, Import a portfolio from JSON, Copy a snapshot link

  Group:    Publishing
  Commands: Publish this portfolio, Open a full preview, Check the page

Commands that cannot run at the moment are not offered.

LIMITS
======

  Limit                    | Value
  -------------------------+------------------------------------------------------------
  Address length           | 2 to 31 characters
  Published portfolio size | Half a megabyte ("Link images rather than embedding them.")
  Timeline fold            | After 5, 8 or 12 entries, or never
  Gallery columns          | 2, 3 or 4
  Feature columns          | 2 or 3
  Work / Projects items    | 3, 6, 9 or 12
  Callout icon             | Up to 4 characters

MESSAGES
========

  Message: Added a … section., Section duplicated., Section removed — Ctrl+Z
           to undo.
  When:    Section actions.

  Message: All sections cleared — Ctrl+Z to undo. and other bulk messages
  When:    Bulk actions.

  Message: … template applied.
  When:    Template chosen.

  Message: HTML downloaded., Portfolio JSON exported., Portfolio imported.
  When:    Export and import.

  Message: That file is not a valid portfolio export.
  When:    Import failed.

  Message: Allow pop-ups to preview., Allow pop-ups to print.
  When:    Pop-ups blocked.

  Message: Public link copied
  When:    Snapshot link copied.

  Message: Avatar updated., Turn the built-in header on first., That picture
           is too large even shrunk. Try a smaller one., That file could not
           be read as a picture.
  When:    Avatar.

  Message: Loaded your portfolio from your account., Loaded the newer draft
           from your account.
  When:    Draft sync.

  Message: Pick something short — this is the whole address., Lower case
           letters, numbers and hyphens, 2 to 31 characters., Checking…, Could
           not check that just now.
  When:    Address field.

  Message: Published at …, Could not publish., Unpublished. The address no
           longer shows anything.
  When:    Publishing.

  Message: That portfolio is larger than half a megabyte. Link images rather
           than embedding them.
  When:    Publish too large.

PORTFOLIO INDEX (HTTPS://AURICARTISAN.COM/PORTFOLIOS/)
======================================================

  Control: Search
  Values:  Words, `"phrases"`, `-exclusions`, hex colours, `tag:`, `in:`,
           `made:here`, `made:elsewhere`, `is:verified`

  Control: Source
  Values:  All, Made here, Elsewhere

  Control: Filters
  Values:  Verified only, Colour (9 families), Craft, Location

  Control: Sort
  Values:  Best match, Recently updated, Newest, Name, A to Z, Most viewed

  Control: View
  Values:  Grid, List

  Control: Paging
  Values:  24 at a time; Show N more of M

  Control: Keys
  Values:  `/` focuses search; `Escape` clears it
