==============================================================================
EXPORT TOKENS
==============================================================================
Browse and search every token, then copy or download the system in any of
twelve formats, and know what each format contains.
Area: Website › Tools › Colour workspace   Updated: 2026-09-25

A design token is a named design decision stored as data, such as
`--color-primary-500: #1f6feb` or `--space-4: 16px`. Tokens let designers and
developers share one source of truth. The Tokens tab lists every token the
system generates and exports them in twelve formats.

THE TOKEN LIST
==============

  - Find token filters the list by name or value as you type, for example
    `primary`, `radius` or `#1f`. Clear shows everything again.
  - Design tokens shows the total and the tokens in groups, each with a count:
    Color · Primary, Color · Secondary, Color · Accent, Color · Neutral, Color
    · Semantic, Color · Roles (light), Color · Roles (dark), Color ·
    Gradients, Typography, Typography · Size, Typography · Leading, Typography
    · Tracking, Spacing, Radius, Shadow, Motion, Z-index and Breakpoints.
  - Colour tokens have a chip. Select any token to copy its value.

The default light-and-dark system has 177 tokens; with Colour mode set to
Light, the fourteen dark role tokens are left out (163).

The names follow Token naming in the brief; see Set the brief
[set-the-brief.txt].

EXPORT
======

Scroll to Export tokens. Select a format pill; the code below it changes, and
its bar shows the file name and language. Three buttons:

  - Copy copies the code shown.
  - Download saves it under the format's file name.
  - Download all (.zip-less bundle) saves every format, one after another with
    a comment line between them, in one text file, `design-system-bundle.txt`.
    It is not a zip archive; split it by hand or download formats one at a
    time.

  Pill:         CSS
  File:         `tokens.css`
  What you get: A `:root` block with every token, and a dark block that
                applies under `[data-theme="dark"]` or `.dark`.

  Pill:         CSS (modern)
  File:         `tokens.modern.css`
  What you get: Colour roles paired with `light-dark()` under `color-scheme:
                light dark`, and every colour restated in `oklch()` inside a
                `@media (color-gamut: p3)` block for wide-gamut screens.

  Pill:         SCSS
  File:         `_tokens.scss`
  What you get: One `$variable` per token and a `$tokens` map.

  Pill:         Tailwind v3
  File:         `tailwind.config.js`
  What you get: A `theme.extend` block: colour ramps, semantic solids, font
                families, font sizes with line heights, spacing, radius and
                shadows.

  Pill:         Tailwind v4
  File:         `theme.css`
  What you get: An `@theme` block.

  Pill:         Figma / W3C
  File:         `design-tokens.json`
  What you get: The W3C Design Tokens format for Figma Variables or Tokens
                Studio.

  Pill:         JSON
  File:         `design-system.json`
  What you get: The whole system with its settings. The most complete export.

  Pill:         TypeScript
  File:         `tokens.ts`
  What you get: The JSON as a typed constant with a `Tokens` type.

  Pill:         Android XML
  File:         `colors.xml`
  What you get: Colour and dimension resources.

  Pill:         SwiftUI
  File:         `DesignTokens.swift`
  What you get: A `Color` extension and dimension constants.

  Pill:         Flutter
  File:         `app_tokens.dart`
  What you get: A class of `Color` and number constants.

  Pill:         Compose
  File:         `Tokens.kt`
  What you get: Jetpack Compose Kotlin values.

The CSS, SCSS, Tailwind and TypeScript files start with a comment naming the
industry, style, target, seed and generation time; the JSON export carries the
same details as data, and the Figma / W3C file has a one-line description.

WHAT EACH FORMAT COVERS
=======================

Not every format carries every token. Check before you choose one.

  Content:                 Colour ramps and semantic colours
  CSS, CSS (modern), Tailwind v4:Yes
  SCSS:                    Yes
  Tailwind v3:             Ramps and semantic solids
  Figma / W3C:             Ramps and semantic solids
  JSON, TypeScript:        Ramps and semantic solids
  Android, SwiftUI, Flutter, Compose:Yes

  Content:                 Light roles
  CSS, CSS (modern), Tailwind v4:Yes
  SCSS:                    Yes
  Tailwind v3:             No
  Figma / W3C:             No
  JSON, TypeScript:        Yes
  Android, SwiftUI, Flutter, Compose:Yes

  Content:                 Dark roles
  CSS, CSS (modern), Tailwind v4:CSS and CSS (modern) only
  SCSS:                    No
  Tailwind v3:             No
  Figma / W3C:             No
  JSON, TypeScript:        Yes
  Android, SwiftUI, Flutter, Compose:Yes

  Content:                 Gradients
  CSS, CSS (modern), Tailwind v4:Yes
  SCSS:                    Yes
  Tailwind v3:             No
  Figma / W3C:             No
  JSON, TypeScript:        Yes
  Android, SwiftUI, Flutter, Compose:No

  Content:                 Font families
  CSS, CSS (modern), Tailwind v4:Yes
  SCSS:                    Yes
  Tailwind v3:             Yes
  Figma / W3C:             Names only
  JSON, TypeScript:        Names only
  Android, SwiftUI, Flutter, Compose:No

  Content:                 Font sizes, spacing, radius
  CSS, CSS (modern), Tailwind v4:Yes
  SCSS:                    Yes
  Tailwind v3:             Yes
  Figma / W3C:             Yes
  JSON, TypeScript:        Yes
  Android, SwiftUI, Flutter, Compose:Yes

  Content:                 Shadows
  CSS, CSS (modern), Tailwind v4:Yes
  SCSS:                    Yes
  Tailwind v3:             Yes
  Figma / W3C:             Yes
  JSON, TypeScript:        Yes
  Android, SwiftUI, Flutter, Compose:No

  Content:                 Leading and tracking
  CSS, CSS (modern), Tailwind v4:Yes
  SCSS:                    Yes
  Tailwind v3:             Line heights with sizes
  Figma / W3C:             No
  JSON, TypeScript:        Yes
  Android, SwiftUI, Flutter, Compose:No

  Content:                 Motion, z-index
  CSS, CSS (modern), Tailwind v4:Yes
  SCSS:                    Yes
  Tailwind v3:             No
  Figma / W3C:             No
  JSON, TypeScript:        Yes
  Android, SwiftUI, Flutter, Compose:No

  Content:                 Breakpoints
  CSS, CSS (modern), Tailwind v4:Yes
  SCSS:                    Yes
  Tailwind v3:             No
  Figma / W3C:             No
  JSON, TypeScript:        Yes
  Android, SwiftUI, Flutter, Compose:Yes

Notes:

  - Native formats (Android, SwiftUI, Flutter, Compose) keep only plain HEX
    colours and pixel dimensions (spacing, radius, font sizes and
    breakpoints). Translucent colours, such as the focus ring and the dark
    theme's borders, are left out; define them by hand on those platforms.
  - The roles `primary-active` and `primary-subtle` appear on the Colour tab
    and in the JSON and TypeScript exports, but not in the other formats.
  - If you set a token prefix or a case other than kebab, the native formats
    may leave out the dimension tokens; export them with the default naming if
    you need them.
  - For a production system, pick one complete format (CSS or JSON) as your
    source of truth and generate the rest from it, rather than mixing formats
    with different coverage.

EXAMPLE
=======

The start of a CSS export:

    /* Auric Artisan · Design System */
    /* Education · Playful · AA · seed #… */
    /* Generated 2026-09-24T10:00:00.000Z */
    :root {
      --color-primary-50: #…;
      --color-primary-100: #…;
      /* … */
    }
    
    /* Dark theme — apply [data-theme="dark"] or .dark on a wrapper */
    [data-theme="dark"], .dark {
      --color-bg: #…;
      /* … */
    }

RELATED
=======

  - Set the brief [set-the-brief.txt] for token naming
  - Share, save and connect [share-save-and-connect.txt]
  - Reference [reference.txt]
