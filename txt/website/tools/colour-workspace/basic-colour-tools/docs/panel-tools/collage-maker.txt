==============================================================================
COLLAGE MAKER (PANEL)
==============================================================================
Put your own images into one of 29 collage layouts, style the grid, apply
filters, add a title and export a 4K PNG, all inside a Basic Color Tools
panel.
Area: Website › Tools › Colour workspace   Updated: 2026-09-25

The Collage Maker panel arranges images from your device into a layout: a
plain grid, an editorial magazine page, a filmstrip, a pinboard and 25 more.
You can set spacing, corner radius, background, borders and a drop shadow,
apply a colour filter to all images or to single cells, add a title, and
export the result as a large PNG. It is useful for mood boards, palette boards
and quick visual references.

This is the compact collage tool inside Basic Color Tools. The site also has a
separate, fuller Collage Maker page; see Image and file tools
[../../../../image-and-file-tools/README.txt].

The panel's Collage Maker is in the Compose category at the Advanced level.

OPEN IT
=======

In the Workspace view, select Collage Maker in Open a tool. It opens with an
empty three-by-two grid.

SCREEN TOUR
===========

Sidebar
-------

  - Layout: 29 layouts.
  - Cols, Rows, Gap and Radius number boxes.
  - Background colour.
  - Border width / color: a number and a colour.
  - Drop shadow check box.
  - Global filter.
  - Title overlay: text and colour.
  - + Add Images, a shuffle button and a clear (bin) button.
  - Export PNG (4K).

Main area: Preview
------------------

A 4:3 preview of the collage. Empty cells read + Add. Under it: Click any cell
to assign an image. Drag from desktop to drop multiple at once. Right-click a
cell to apply a per-cell filter. Dropping files is not supported in the
current version, and a plain right-click opens the site's own menu; use the
methods in the tasks below.

LAYOUTS
=======

Cols and Rows shape the layouts marked "your grid". The others have a fixed
number of cells.

  Layout: Grid
  Cells:  your grid
  Notes:  Equal cells, Cols by Rows.

  Layout: Brick
  Cells:  your grid
  Notes:  Cols by Rows.

  Layout: Mosaic
  Cells:  your grid
  Notes:  A finer grid with a repeating pattern of large and small cells.

  Layout: Hero
  Cells:  your grid (at least 2)
  Notes:  The first cell is large, the rest fill around it.

  Layout: Split
  Cells:  2
  Notes:  Two large halves.

  Layout: Two-up
  Cells:  2
  Notes:  Two side by side.

  Layout: Triptych
  Cells:  3
  Notes:  Three side by side.

  Layout: Sidebar
  Cells:  4
  Notes:  One large image and three small ones.

  Layout: Magazine
  Cells:  5
  Notes:  Editorial page with one large image.

  Layout: Feature
  Cells:  4
  Notes:  A wide feature image above smaller ones.

  Layout: Panorama
  Cells:  5
  Notes:  A wide image above four small ones.

  Layout: Banner
  Cells:  4
  Notes:  One large image beside three tall, narrow ones.

  Layout: Golden
  Cells:  4
  Notes:  One dominant image and diminishing ones.

  Layout: Story
  Cells:  4
  Notes:  A tall format with a wide top image.

  Layout: Poster
  Cells:  4
  Notes:  A large image above three small ones.

  Layout: Frame
  Cells:  5
  Notes:  Images framing a centre image.

  Layout: Quilt
  Cells:  10
  Notes:  Mixed sizes.

  Layout: Tiles
  Cells:  9
  Notes:  Mixed sizes.

  Layout: Checkerboard
  Cells:  16
  Notes:  Four by four; alternate cells are slightly faded.

  Layout: Diagonal
  Cells:  7
  Notes:  Cells stepping diagonally.

  Layout: Staircase
  Cells:  5
  Notes:  Rows that grow wider, like steps.

  Layout: Masonry
  Cells:  12
  Notes:  Four columns of varied heights.

  Layout: Columns
  Cells:  4
  Notes:  Four tall columns.

  Layout: Rows
  Cells:  3
  Notes:  Three wide rows.

  Layout: Contact sheet
  Cells:  20
  Notes:  Five by four small frames.

  Layout: Pinboard
  Cells:  12
  Notes:  Four by three, each image slightly tilted.

  Layout: Polaroid
  Cells:  Cols (at least 3)
  Notes:  One row, images tilted like prints.

  Layout: Strip
  Cells:  Cols (at least 3)
  Notes:  One row.

  Layout: Filmstrip
  Cells:  5
  Notes:  Five frames in a row.

TASKS
=====

Make a mood board
-----------------

  1. Choose a Layout, for example Magazine.
  2. Select + Add Images and choose several images at once. They fill the
     empty cells in order. Images beyond the layout's number of cells are
     ignored.
  3. To place or replace a single image, select that cell and choose one file.
  4. Select the shuffle button to mix the order of the images.
  5. Adjust Gap, Radius, Background, border and Drop shadow until it looks
     right.

Result: your images arranged in the chosen layout in the preview.

Apply filters
-------------

  1. Choose a Global filter to apply one look to every image: None, Grayscale,
     Sepia, Invert, Blur, Warm, Cool, Contrast or Vintage.
  2. To give one cell its own filter, hold `Shift` and right-click that cell.
     Each `Shift` + right-click moves the cell to the next filter in the list
     above, and a message such as Filter: sepia appears. A cell's own filter
     overrides the global filter.

If you have set the site's right-click menu to use the browser's menu, a plain
right-click cycles the filter instead. See Site features
[../../../../../site-features/README.txt].

Result: every image carries the look you chose, with individual exceptions.

Add a title and export
----------------------

  1. Type a Title overlay and choose its colour.
  2. Select Export PNG (4K).

The title does not appear in the preview. It is drawn only in the exported
image: bold, centred near the top, with a soft shadow.

Result: `collage.png` downloads, with the preview's proportions, the
background colour, rounded corners, borders, filters and title. When the
preview is at least 800 pixels wide, the image is 3,840 pixels wide. When the
preview is narrower, the image is 4.8 times the preview's size and so less
than 3,840 pixels wide; widen or maximise the panel before exporting to get
the full size.

CONTROLS
========

  Control:         Layout
  What it does:    Chooses the arrangement.
  Values or range: 29 layouts
  Default:         Grid

  Control:         Cols
  What it does:    Columns for grid-based layouts; images per row for Polaroid
                   and Strip.
  Values or range: 1 to 6
  Default:         3

  Control:         Rows
  What it does:    Rows for grid-based layouts.
  Values or range: 1 to 6
  Default:         2

  Control:         Gap
  What it does:    Space between cells, in pixels.
  Values or range: 0 to 40
  Default:         6

  Control:         Radius
  What it does:    Corner rounding of cells, in pixels.
  Values or range: 0 to 40
  Default:         8

  Control:         Background
  What it does:    Colour behind the cells.
  Values or range: Any colour
  Default:         #101010

  Control:         Border width
  What it does:    Inner border on each cell, in pixels.
  Values or range: 0 to 20
  Default:         0

  Control:         Border colour
  What it does:    Colour of that border.
  Values or range: Any colour
  Default:         #FFFFFF

  Control:         Drop shadow
  What it does:    Shadow under each cell in the preview.
  Values or range: On or off
  Default:         On

  Control:         Global filter
  What it does:    Filter for every image.
  Values or range: None, Grayscale, Sepia, Invert, Blur, Warm, Cool, Contrast,
                   Vintage
  Default:         None

  Control:         Title overlay
  What it does:    Text drawn at the top of the export.
  Values or range: Any text
  Default:         Empty

  Control:         Title colour
  What it does:    Colour of the title.
  Values or range: Any colour
  Default:         #FFFFFF

  Control:         + Add Images
  What it does:    Adds several images to empty cells.
  Values or range: Image files
  Default:         —

  Control:         Cell
  What it does:    Select to place one image in that cell.
  Values or range: One image file
  Default:         —

  Control:         `Shift` + right-click a filled cell
  What it does:    Cycles that cell's filter.
  Values or range: Same list as Global filter
  Default:         —

  Control:         Shuffle
  What it does:    Randomises the image order.
  Values or range: —
  Default:         —

  Control:         Clear (bin)
  What it does:    Removes all images.
  Values or range: —
  Default:         —

  Control:         Export PNG (4K)
  What it does:    Downloads the collage as `collage.png`.
  Values or range: 3,840 pixels wide when the preview is 800 pixels wide or
                   more
  Default:         —

ACCURACY AND LIMITS
===================

  - Images are read from your device in the browser and are not uploaded. Very
    large images or many images make the export slower.
  - The export redraws the collage at high resolution from the preview's
    layout. The drop shadow and the slight fade of alternate Checkerboard
    cells appear only in the preview; the pinboard and polaroid tilts are not
    drawn in the export either.
  - Images are cropped to fill their cells, keeping their centre.
  - Open panels are not saved when you reload the page. Export before you
    leave.

RELATED
=======

  - Panel tools [README.txt]
  - Collage Maker page and other image tools
    [../../../../image-and-file-tools/README.txt]
  - Troubleshooting [../troubleshooting.txt]
