# Code tools — plain-text edition

The tools in the Code space — Inspect, Tokens and Snippets — and every code format the extension can export. Every file here is the plain-text edition of the Markdown document with the same name in `md/browser-extension/docs/code/`; start with `README.txt`.

## Files

| File | What it covers |
| --- | --- |
| [README.txt](README.txt) | The tools in the Code space — Inspect, Tokens and Snippets — and every code format the extension can export. |
| [code-export.txt](code-export.txt) | Every format the extension can copy or download, which tool produces it, and what the output looks like. |
| [inspect.txt](inspect.txt) | Select an element on the page and read its full snapshot in the popup — box model, styles, matched rules, accessibility, DOM, locators — and try CSS on it live. |
| [snippets.txt](snippets.txt) | Keep reusable code in the extension's on-device vault — save from tools or paste your own, search, filter by site, copy and export. |
| [tokens.txt](tokens.txt) | Read a web page's design tokens — CSS custom properties, font families and type scale — and export them as CSS, W3C tokens, a Tailwind @theme block or JSON. |

Updated 2026-09-25.
