# Text stays LF everywhere so the plain-text edition reads the same on every system.
* text=auto eol=lf

# Generated binary editions.
*.pdf  binary
*.epub binary
*.docx binary
*.png  binary

# The CSV files carry a byte-order mark and CRLF so spreadsheets open them cleanly.
*.csv -text
